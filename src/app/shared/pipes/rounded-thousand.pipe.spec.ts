import { RoundedThousandPipe } from './rounded-thousand.pipe';

describe('RoundedThousandPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundedThousandPipe();
    expect(pipe).toBeTruthy();
  });
});
